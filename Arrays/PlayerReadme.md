# 🏏 Player Management Program (Java)

This Java program demonstrates how to **create and manage multiple Player objects** using **arrays, constructors, and the `toString()` method**.  
It allows the user to input details of multiple players (ID, Name, Base Price) and then displays all player information.

---

## 📘 Features

- Accepts user input for multiple Player objects.  
- Uses a **parameterized constructor** to initialize player details.  
- Displays player information using **`toString()`** override.  
- Demonstrates **array of objects** concept in Java.  

---

## 🧩 Class Details

### **Class: `Player`**

#### Fields
| Field | Type | Description |
|--------|------|-------------|
| `id` | `int` | Player ID |
| `name` | `String` | Player Name |
| `price` | `double` | Player Base Price |

#### Constructor
```java
public Player(int id, String name, double price)
##output
<img width="835" height="653" alt="Screenshot 2025-11-03 230651" src="https://github.com/user-attachments/assets/a6b3301c-9cd9-42d8-84f4-d764836cd1f0" />
